// ROOT
export const root = '';


// AUTH
export const auth = `${root}/auth`;
export const authLogin = `${auth}/login`;
export const authVK = `${auth}/vk`;


// USER
export const user = `${root}/user`;
export const userProfile = `${root}/userProfile`;


export default {
    auth,
    authLogin,
    authVK,


    user,
    userProfile,
}